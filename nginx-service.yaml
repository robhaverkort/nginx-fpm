---
apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: nginx
  name: nginx
  namespace: nginx-fpm
spec:
  ports:
    - name: http-nginx
      port: 80
      protocol: TCP
      appProtocol: http
      targetPort: http-nginx
  selector:
    app.kubernetes.io/name: nginx
status:
  loadBalancer: {}
